; /.env
; Set to true to use mock data (no database server required)
USE_MOCK=false

; PostgreSQL config (only used if USE_MOCK is false)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cms_db
DB_USER=postgres
DB_PASS=root
DB_DRIVER=pgsql

BASE_PATH=/MindCloud-SCMS