# Application environment
APP_ENV=development

# Base path (used for subfolder deployments)
BASE_PATH=/MindCloud-SCMS

# Pagination
UI_PER_PAGE_DEFAULT=10

# Mock data toggle
USE_MOCK=true

# Database configuration (only used if USE_MOCK=false)
DB_DRIVER=pgsql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cms_db
DB_USER=postgres
DB_PASS=changeme
